import { CheckSquare } from 'lucide-react'
import { PlaceholderPage } from '@/components/placeholder-page'

export function TasksPage() {
  return (
    <PlaceholderPage
      icon={CheckSquare}
      title="Tasks"
      description="Create, assign, and track every wedding to-do by category, priority, and due date."
    />
  )
}
