import { Store } from 'lucide-react'
import { PlaceholderPage } from '@/components/placeholder-page'

export function VendorsPage() {
  return (
    <PlaceholderPage
      icon={Store}
      title="Vendors"
      description="Keep every vendor's contact details, quotes, and contracts organized in one directory."
    />
  )
}
