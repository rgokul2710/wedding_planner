import { CalendarDays } from 'lucide-react'
import { PlaceholderPage } from '@/components/placeholder-page'

export function EventsPage() {
  return (
    <PlaceholderPage
      icon={CalendarDays}
      title="Events & Timeline"
      description="See every ceremony and function — engagement, haldi, sangeet, wedding, reception — on one timeline."
    />
  )
}
